Template pattern defines the skeleton of the algorithm in a method, deferring some steps to subclasses. 
Template method let subclasses to redefine certain steps of the algorithm without changing the algorithms structure.


Subclasses decide how to implement steps in an algorithm.


Don’t call us, we will call you
